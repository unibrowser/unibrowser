"""
Need to ensure that python-dev and python3-dev are installed to install datetime
Need to ensure that we have twitter tokens (perhapes we just download some and hard code test cases so that we don't
   have to store keys on our test server...)
"""
import nltk
nltk.download('stopwords')
nltk.download('punkt')
